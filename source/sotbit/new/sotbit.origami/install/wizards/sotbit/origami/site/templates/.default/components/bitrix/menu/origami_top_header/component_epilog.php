<script>
    $topBar = $('.category-menu');
    $button = $('.category-menu .responsive-hidden-button');
    $visibleLinks = $('.category-menu .visible-links');
    $hiddenLinks = $('.category-menu .hidden-links');
    responsiveBreaks = []; // Empty List (Array) on initialization
    updateTopBar($topBar, $button, $visibleLinks, $hiddenLinks, responsiveBreaks);
</script>